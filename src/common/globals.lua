local globals = {
  playerName = 'Felipe',
  language = 'portugues',
  baseScreenWidth = 1024,
  resolution = 16/9
};

globals.baseScreenHeight = globals.baseScreenWidth * 1 / globals.resolution

return globals;
